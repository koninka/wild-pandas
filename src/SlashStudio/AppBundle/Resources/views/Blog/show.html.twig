{% extends '::base.html.twig' %}

{%  block body %}
  <h1>{{ post.title }}</h1>
  <p>{{ post.subtitle }}</p>
  <p>{{ post.text }}</p>
  {% media post.image, 'main' %}
  {{ post.createdAt.date }}
{% endblock %}
