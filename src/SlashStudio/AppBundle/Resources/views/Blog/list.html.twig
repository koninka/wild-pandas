{% extends '::base.html.twig' %}

{%  block body %}
  <h1>{{ 'navigation.blog' | trans({}, 'navigation') }}</h1>
  {% for post in posts %}
    <div>
      {{ post.title }}
      {{ post.description }}
      {{ post.text }}
    </div>
  {% endfor %}
{% endblock %}
