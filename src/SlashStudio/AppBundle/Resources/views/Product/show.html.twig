{% extends '::base.html.twig' %}

{% block body %}
  <h1>{{ product.name }}</h1>
  {# TO DO: need to translate #}
  <div>Описание: {{ product.description }}</div>
  <div>Цена: {{ product.price }}</div>
{% endblock %}
