{% extends '::base.html.twig' %}

{% block body %}
  <h1>{{ 'navigation.media_about' | trans({}, 'navigation') }}</h1>
  {% for p in posts %}
    <div>
      <b>{{ p.title }}</b>
      <p>{{ p.subtitle }}</p>
    </div>
  {% endfor %}
{% endblock %}
